interface:
  display_name: "TokenMax Fleet Orchestrator"
  short_description: "Run resumable, gated SEO/GEO fleets one site at a time"
  default_prompt: "Use $tokenmax-fleet-orchestrator to audit this fleet, run the next eligible site through hard gates, and report exact state."
